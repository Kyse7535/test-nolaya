[build]
  command = "npm run build"
  publish = "dist"
  # Skip builds when the commit doesn't touch this package (monorepo)
  ignore = "git diff --quiet $CACHED_COMMIT_REF $COMMIT_REF ."

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
